# CLAUDE.md

The invariants this codebase is built around live in
[ARCHITECTURE.md](ARCHITECTURE.md) — read it first.

They are project documentation, not tool configuration, which is why they are
named for what they contain. This file exists only so agentic tools that look
for `CLAUDE.md` by convention still find their way there.
